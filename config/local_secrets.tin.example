#nop =====================================================
#nop LOCAL SECRETS (EXAMPLE)
#nop Copy this file to `config/local_secrets.tin` and fill in real passwords.
#nop This file is gitignored — only the .example is tracked.
#nop =====================================================

#nop Add one line per character. The variable name must match what the character
#nop profile reads (e.g. char/mutiny.tin reads $mutiny_password).
#VARIABLE {mutiny_password}  {your_mutiny_password_here}
#VARIABLE {haenym_password}  {your_haenym_password_here}
#VARIABLE {prodigy_password} {your_prodigy_password_here}
#VARIABLE {rancor_password}  {your_rancor_password_here}
