#nop =====================================================
#nop LOCAL SECRETS (EXAMPLE)
#nop Copy this file to `config/local_secrets.tin` and fill in real credentials locally
#nop This file is an example and should NOT contain real secrets in source control
#nop =====================================================

#nop Local secrets example (do not commit real secrets)

#nop Copy this file to `config/local_secrets.tin` and fill in your real passwords.

#VARIABLE {mutiny_password} {your_mutiny_password_here}
#VARIABLE {haenym_password} {your_haenym_password_here}

#nop Optional: add other per-character variables you need, e.g.:
#VARIABLE {my_other_char_password} {password_here}
